{
  "name": "mycelia-preview",
  "private": true,
  "type": "module",
  "description": "Local render harness for the Mycelia Shopify theme: renders JSON templates with fixture data via LiquidJS + Shopify shims so Lighthouse, axe and grid tests can run without a store.",
  "scripts": {
    "build": "node render.mjs",
    "serve": "node serve.mjs",
    "test": "node tests/run.mjs"
  }
}
